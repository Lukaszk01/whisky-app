class Customer
    attr_accessor :id
    attr_reader :name, :address
    
    def initialize(attributes = {})
        @id = attribute[id]
        @name = attribute[name]
        @address = attribute[address]
    end
end